#include<stdio.h>
int main()
{
int a[10],n=7,i;
printf("Enter the array elements:");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
printf("Print the array elements:");
for(i=0;i<n;i++)
printf("a[%d]=%d",i,a[i]);
return 0;
}
